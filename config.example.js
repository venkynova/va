// Copy this file to config.js and edit the values.
// Never commit a real API key to GitHub.

window.VENKY_CONFIG = {
  // "demo", "ollama", or "gemini"
  provider: "demo",

  // Ollama (local AI, no API bill)
  endpoint: "http://localhost:11434",
  model: "gemma3:4b",

  // Gemini (optional; keep this empty until you have a key)
  apiKey: "",
};
